version: "3.5"

services:
    mps_backend_nginx:
        build:
            context: .
            dockerfile: Dockerfile.nginx
        image: mps_backend/nginx
        container_name: mps_backend_nginx_live
        hostname: nginx
        stdin_open: true
        tty: true
        ports:
            - "7777:5555"

networks:
    default:
        name: mps_backend_live